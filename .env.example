# Example environment variables (do NOT commit real secrets)

# SMTP for sending emails (optional)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Jordan's Crossing <no-reply@jordanscrossing.org>"
INTAKE_EMAIL=

# Admin UI credentials (for local/demo only)
ADMIN_USER=
ADMIN_PASS=
